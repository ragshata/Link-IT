# LinkIT — Telegram-бот для IT-нетворкинга

**LinkIT** — это Telegram-бот для разработчиков и людей из IT, который помогает:

- находить **напарников** для pet-проектов и совместного обучения;
- находить **менторов** или тех, кого можно менторить;
- подбирать **джунов/мидлов** в свои проекты;
- искать и публиковать **проекты**, куда можно подключиться.

Без романтики и дейтинга — только код, обучение и совместная работа.

Бот: `@GoLinkITBot` (dev/стейджинг экземпляр, не прод).

---

## Основные возможности

### 1. Профиль пользователя

Пользователь один раз заполняет профиль:

- имя (можно взять из Telegram);
- аватар (можно подтянуть из Telegram);
- роль в IT (backend, frontend, fullstack, mobile, data, QA, product, design и т.д.);
- язык/стек (Python, Go, Node.js, React и т.п.);
- фреймворки:
  - для **fullstack** — можно выбрать **несколько** (multi-select);
  - для остальных ролей — основной фреймворк;
- общие навыки (Git, SQL, Docker, Linux и т.д., тоже multi-select + свои варианты);
- цели:
  - найти ментора / стать ментором;
  - найти напарника;
  - найти проект;
  - найти джуна/помощника;
- краткое “О себе”.

Профиль можно в любой момент **отредактировать**, при этом:

- редактирование идёт пошагово (wizard);
- есть кнопка **«Отменить редактирование»**, которая:
  - сбрасывает черновик,
  - оставляет в базе старые данные,
  - показывает актуальный профиль.

---

### 2. Лента разработчиков

Раздел **«👨‍💻 Лента специалистов»** (название может отличаться, логика такая):

- пользователю показывается **карточка разработчика**:
  - аватар;
  - имя;
  - роль;
  - стек и фреймворки;
  - навыки;
  - цель в LinkIT;
  - “О себе”.
- внизу инлайн-кнопки:
  - `🏆 Награды` (пока заглушка);
  - `🤝 Отправить заявку` (заявка на общение/сотрудничество);
  - `⬅️ Предыдущий` / `➡️ Следующий`.

При листании ленты:

- нажатие на **«Следующий» / «Предыдущий»**:
  - отправляет новую карточку,
  - **удаляет предыдущую**, чтобы в чате не было “простыней”.

---

### 3. Заявки на общение

При нажатии на кнопку “отправить заявку”:

1. У выбранного пользователя в личку приходит уведомление:
   - текст с пояснением;
   - **аватар** отправителя;
   - профиль отправителя в формате “карточки”.
2. Внизу — кнопки:
   - `✅ Принять`;
   - `❌ Отклонить`.

Если заявка **принята**:

- обеим сторонам приходит уведомление,
- раскрываются **контакты** (Telegram @username друг друга),
- можно переходить к общению.

Если **отклонена** — отправителю приходит вежливый отказ.

---

### 4. Проекты

Раздел **«🚀 Лента проектов»** + **создание проекта**.

#### Регистрация проекта

Через кнопку **«🆕 Новый проект»** пользователь проходит мастер:

1. Обложка проекта (фото) — опционально.
2. Название проекта.
3. Стек (можно выбрать **несколько** технологий).
4. Описание идеи и текущего состояния.
5. Кого ищем:
   - backend / frontend / fullstack / mobile / data / QA / product / design и т.д.;
   - можно выбрать **несколько ролей**.
6. Уровень:
   - `Junior / Middle / Senior / Любой`.
7. Дополнительные детали:
   - формат участия (вечера/выходные),
   - занятость,
   - важные нюансы.

Проект появляется в **ленте проектов** у других пользователей.

#### Лента проектов

В ленте проектов по одному показываются карточки:

- обложка (если есть);
- название;
- стек;
- описание идеи;
- кого ищем;
- желаемый уровень;
- дополнительные детали.

Кнопки:

- `🤝 Откликнуться на проект`;
- `⬅️ Предыдущий` / `➡️ Следующий`.

Механика отклика — та же, что и для заявок между пользователями:
заявка летит владельцу проекта, он принимает/отклоняет, при принятии открываются контакты.

---

### 5. Главное меню бота

После регистрации у пользователя появляется **Reply-клавиатура**:

- `👥 Лента разработчиков`
- `🚀 Лента проектов`
- `🆕 Новый проект`
- `👤 Профиль`

Команды:

- `/start` — главное меню или запуск первой регистрации;
- `/profile` — показать профиль;
- `/edit_profile` — редактировать профиль;
- `/help` — краткая справка.

---

## Технологический стек

- **Python** 3.11+ / 3.12
- **aiogram 3.x** — Telegram Bot API
- **SQLAlchemy** (async) — работа с БД
- **PostgreSQL** (по умолчанию) или другая БД, поддерживаемая SQLAlchemy
- Асинхронные сессии: `AsyncSession` из `sqlalchemy.ext.asyncio`

---

## Архитектура проекта

Условная структура:

```bash
LinkIT/
├── main.py               # входная точка приложения
├── settings.py           # конфигурация (токен бота, DATABASE_URL и т.п.)
├── db.py                 # инициализация БД (engine, SessionLocal, Base)
├── models.py             # SQLAlchemy-модели (Profile, Project, ConnectionRequest и т.д.)
├── repositories.py       # слой доступа к данным (CRUD для профилей, проектов, заявок)
├── constants.py          # константы и опции (ролей, стеков, фреймворков, навыков, целей)
├── handlers/
│   ├── __init__.py
│   ├── start.py          # /start, главное меню
│   ├── profile.py        # профиль, регистрация/редактирование
│   ├── devfeed.py        # лента разработчиков
│   ├── projects.py       # регистрация проектов и лента проектов
├── services/
│   ├── __init__.py
│   ├── profiles.py       # бизнес-логика работы с профилями
│   ├── projects.py       # бизнес-логика по проектам
│   ├── connections.py    # заявки, коннекты между пользователями
├── views/
│   ├── __init__.py
│   ├── profiles.py       # форматирование текста профиля
│   ├── projects.py       # форматирование карточек проектов
├── middlewares/
│   ├── __init__.py
│   ├── db.py             # middleware для прокидывания AsyncSession в хендлеры
├── create_db.py          # скрипт для создания таблиц (Base.metadata.create_all)
├── requirements.txt
└── README.md
